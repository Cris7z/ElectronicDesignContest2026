# 2026 H 题硬件及接线清单

> 版本：v0.6，2026-07-30
> 本文件是项目的唯一硬件真值表；后续固件默认直接按本表实施，不重复扫描
> 原始厂家资料。只有硬件变更、实物行为冲突或“待实测”项目才重新核查。
> 首次上电前仍须根据 S27F/S28A、C07A 版本和 D36A 实物丝印逐线做通断、
> 电压和有效极性检查。面向编码代理的强制约束见 [`AGENTS.md`](AGENTS.md)。

## 1. 硬件清单

| 硬件 | 状态 | 用途与结论 |
|---|---|---|
| WHEELTEC C07A（MSPM0G3507）+ S27F/S28A 模块化控制器 | 已购 | 唯一实时控制主机；底板版本按实物丝印登记 |
| P03B 12 V→5 V 稳压模块 | 控制器内含 | 默认给控制器、MPU6050 和 CD4051 八路灰度模块供电 |
| MPU6050 模块 | 控制器内含 | 保留；PA0/PA1 I²C、PA7 INT |
| 外接四针 I²C OLED、启动键 BLS、LED | OLED 已接入，其余控制器内含 | 新 OLED 作启动计时/故障显示；原 C07A 四线 OLED 不再使用 |
| D103A/TB6612 双路驱动模块 | 控制器内含、当前接入 | 当前左右轮驱动；A=PB2/PA14/PA13 为物理左轮，B=PB3/PA16/PA17 为物理右轮（2026-07-31 重测） |
| 主从一体蓝牙模块 | 控制器内含 | 已登记且必须拔下；H8 的 PB6/PB7 当前保留，不由 Q2 自动分配 |
| D157B 双 AT8236 电机驱动板 | 已购、当前不接入 | 不与板载 TB6612 并用 |
| 32 cm × 24 cm 三轮差速底盘 | 已购 | 横向距 25 cm 上限仅余 1 cm，装车必须过检查框 |
| MG513XP28 12 V 编码减速电机 ×2 | 已购 | 左右轮；CPR、减速比、堵转电流待实测 |
| CD4051 八路灰度模块 | 已接入 | 主循迹；5 V 供电，PA12/PA27/PB16 选址，PB17 ADC 采 OUT |
| HiWonder LineFollower_8CH / LF04 | 已购、当前拔除 | 不与占用 U3 的 CD4051 灰度模块并接 |
| D36A 步进驱动器 | **型号已确定** | 球杆 STEP/DIR/EN 执行器 |
| 42 步进电机 + MS42CG A/B/PWM 编码器 | 已购 | 球杆角度闭环 |
| 25 cm PPR 管、直径约 1 cm 钢球、铰链和传动件 | 已有/已购 | 球杆机械系统 |
| 01Studio CanMV K230 标准版套装，1 GB 起步 | 拟购 | 最终视觉和无线图传；1 GB 需经压力测试 |
| 套装标配 GC2093 70°、24P 摄像头 | 随 K230 标配 | 同一相机同时供球检测和 RTSP |
| 16 GB MicroSD、Mini 读卡器、Type-C 线、4P 转接线、散热片、亚克力底板 | 随 K230 套装 | 启动盘、开发、散热和串口接线 |
| 2.4 GHz AP/热点 | 可选 | 现有路由器、手机/电脑热点或 K230 AP 均可 |
| 场外笔记本 + VLC/OBS | 已有/待确认 | 实时显示、录像和立即回放 |
| K230 5 V 电源 | 待负载测试 | 台架先用 Type-C；P03B 不通过压力测试时才另加 ≥2 A 降压 |
| 3S/12 V 动力电池、总开关、保险和星形分配 | 待核算/补齐 | 依据轮堵转、步进锁止和 K230 峰值电流确定 |
| 24P、15 cm GC2093 延长排线 | 视装车实测选购 | 让相机上置、K230 主板低位安装 |

暂不采购第二套 FPV。只有 K230 连续两小时联合压力测试无法通过时，才考虑
独立图传备份。

### 1.1 模块化控制器物理结构

```text
C07A / MSPM0G3507 核心板
  └─H1/H2 → S27F 或 S28A 模块化底板（具体丝印待实物登记）
      ├─H15/H16 → P03B 12 V→5 V（保留）
      ├─H5 → MPU6050（保留，PA0/PA1/PA7 不分接）
      ├─原 OLED 子板 → PA28/PA31/PB14/PB15（停用）
      ├─外接 I²C OLED → PA1/PA0（I2C0，与 MPU6050 共线）
      ├─H9/H12 → D103A/TB6612（当前驱动左右轮）
      ├─H8 蓝牙座 → 当前空置（蓝牙物理拔下）
      ├─U3 → CD4051 灰度板 AD2/AD1/AD0/OUT
      ├─D157B → 当前不接入
      └─D36A + MS42CG → 暂不接入（与 TB6612 PA13/PA14 冲突）
```

“控制器内含”表示模块属于已购整套装配，不表示所有模块都继续插在底板上。
当前保留 P03B、MPU6050、外接 I²C OLED、按键、LED、CD4051 八路灰度和 D103A/TB6612；
蓝牙已拔下。D157B、D36A 和 MS42CG 不接入当前底盘驱动验证。

## 2. 供电接线

```text
3S/12 V 动力电池
  └─总开关 + 可更换保险
      └─星形电源分配点
          ├─12 V → 板载 TB6612 → 左右 MG513XP28
          ├─12 V → D36A → 42 步进电机（当前断开）
          ├─12 V → P03B 5 V
          │     → 模块化控制器 + MPU6050 + CD4051 八路灰度传感器
          └─K230 5 V
                → 台架 Type-C
                → 正式装车优先验证 P03B；不合格再用独立 ≥2 A 降压
```

- P03B 供控制器逻辑与传感器；当前 TB6612 使用独立的电机动力输入。
- K230 接入 P03B 前必须做整机满载纹波、压降和温升测试；未通过才增加独立降压。
- K230 官方要求严格 5 V，严禁接入 3.3 V 或高于 5 V 的电源。
- K230 的 UART 4P 座只传信号；其中 3V3 不作为主板供电输入。
- 所有信号地共地，但电机和步进动力回流不得穿过 K230/C07A 地线。
- 电池容量、C 倍率和保险值必须根据实测峰值确定，并留至少 30% 余量。

## 3. C07A/MSPM0G3507 首版引脚

| 功能 | MSPM0 引脚/资源 | 对端与备注 |
|---|---|---|
| 左电机 | PB2 / TIMA1_CCP0，PA14 / PA13 | TB6612 A 路 PWMA、AIN1、AIN2（2026-07-31 重测） |
| 右电机 | PB3 / TIMA1_CCP1，PA16 / PA17 | TB6612 B 路 PWMB、BIN1、BIN2（2026-07-31 重测） |
| 左轮编码器 A/B | PA25 / PA26 | GPIO 中断软件正交计数；物理前进符号为 -1 |
| 右轮编码器 A/B | PB20 / PB24 | GPIO 中断软件正交计数；物理前进符号为 +1 |
| MPU6050 SDA/SCL/INT | PA0 / PA1 / PA7，H5 | 与外接 OLED 共用 I²C0；H5 不给灰度模块分线 |
| 灰度 AD2 / AD1 / AD0 | PA12 / PA27 / PB16 | CD4051 地址选择，CH1..CH8=000..111 |
| 灰度 OUT | PB17 / ADC1_A1_4 | 已测白约 170 ADC、黑 4095 ADC；仍只允许 0..VDD 输入 |
| 灰度 5V/GND | 底板 5V/GND | 与 MSPM0 共地；U3 被占用，LF04 当前拔除 |
| 外接 OLED SDA/SCL | PA0 / PA1，I²C0 | 四针 SSD1306 兼容 OLED，默认 0x3C；VCC 只能接 3.3 V |
| 启动键 BLS | PA18 | R8=47 kOhm 外部下拉；实测松开=0、按下=1；短按松手起跑、长按约 1 s 急停 |
| 状态 LED | PB9 | 运行/故障指示 |
| 5 ms 控制节拍 | TIMG7，无外部引脚 | TIMA1 已给 TB6612 双路 PWM |
| 调试 UART | PA10 TX / PA11 RX，UART0 | 115200；输出 ADC/灰度/控制诊断 |
| SWD | PA19 / PA20 | 下载和调试，保留 |

TB6612 每路使用一个 PWM 与两个方向输入：`IN1/IN2=00` 滑行，`01/10` 定方向，
`11` 短刹车；PWM 为 0 时必须保持滑行。

### 3.0 当前实装接线（2026-07-31 实测）

下表只描述当前装车、已实测可用的连接；箭头左侧是 C07A/MSPM0 端。未列出的
D157B、D36A、MS42CG、K230 与 LF04 均不得并接到本车当前接口。

| 功能 | C07A/MSPM0 端 | 模块端 / 实装结论 |
|---|---|---|
| 左驱动轮 | `PB2/TIMA1_CCP0`、`PA14/PA13` | 板载 D103A/TB6612 **A** 路：PWMA、AIN1、AIN2；正命令为实物前进（2026-07-31 重测） |
| 右驱动轮 | `PB3/TIMA1_CCP1`、`PA16/PA17` | 板载 D103A/TB6612 **B** 路：PWMB、BIN1、BIN2；正命令为实物前进（2026-07-31 重测） |
| 左轮编码器 | `PA25/PA26` | 左轮 A/B；物理前进计数符号为 `-1` |
| 右轮编码器 | `PB20/PB24` | 右轮 A/B；物理前进计数符号为 `+1` |
| CD4051 供电 | 底板 `5V/GND` | 灰度板 `VCC/GND`；必须与 C07A 共地 |
| CD4051 地址 | `PA12→AD2`、`PA27→AD1`、`PB16→AD0` | 地址 `000..111` 依次选择 R0..R7；R0 为行驶方向左侧 |
| CD4051 模拟输出 | `PB17/ADC1_A1_4←OUT` | 实测白约 170 ADC、黑 4095 ADC；不得接到 5 V 以上 |
| 外接 OLED | `GND→GND`、`3.3V→VCC`、`PA1→SCL`、`PA0→SDA` | 四针 SSD1306 兼容 OLED，I2C0 地址 `0x3C`；与 MPU6050 `0x68` 共线 |
| 启动键 BLS | `PA18` | 板载 SW3；R8=47 kΩ 外部下拉，松开=0、按下=1，无需额外飞线 |

### 3.1 外接 I²C OLED

- 接线按丝印：`GND→C07A GND`、`VCC→C07A 3.3 V`、`SCL→PA1`、`SDA→PA0`。
  **不得把 OLED VCC 接到 H5 的 5 V。** 很多四针板的 I²C 上拉电阻接 VCC，接
  5 V 会把 PA0/PA1 拉到 5 V，损坏 MSPM0。
- 固件使用 I²C0 400 kHz、7 位地址 `0x3C`；它与 H5 MPU6050 (`0x68`) 共总线，
  地址不同，不构成 GPIO 冲突。若实物扫描得到 `0x3D`，只改 OLED 地址常量后重烧。
- 原 C07A U5 四线 OLED 的 `PA28/PA31/PB14/PB15` 已停用；不接新屏，也不再驱动。

### 3.2 CD4051 八路灰度 ADC 循迹

- 模块：用户当前装车的 CD4051 八路灰度板；5 V/GND 共地，阵列位于车轴前方。
  它替代 HiWonder UART/I²C 板，后者拔除。
- 固定接线：`AD2→PA12`、`AD1→PA27`、`AD0→PB16`、`OUT→PB17/ADC1_A1_4`。
  厂商通用例程的 PA14/PA15/PA16/PA17 不可照抄：PA14、PA16、PA17 已属于
  TB6612 方向控制，PA15 不在本次资源表内。
- 实测白底约 170 ADC、黑线 4095 ADC，故当前模块以黑线为高强度模拟输出。PB17
  只允许 `0..VDD`：更换模块或改线后，若最大值含余量超过 `0.9×VDD`，先采用
  OUT—15 kΩ—PB17、PB17—20 kΩ—GND 分压，再重新做 ADC 标定。禁止直接把未知
  5 V 输出接入 MCU。
- CD4051 若以 5 V 供电而其 VIH 不保证接受 3.3 V，必须在 AD0/AD1/AD2 前加
  5 V 供电的 74AHCT125。逐地址确认 `000..111` 对应 CH1..CH8，确认物理左右
  和每个通道的极性。
- 固件每 5 ms 扫描全部八通道；默认切换后 100 µs、丢弃一次 ADC 转换、平均四次。
  以 1 ms 参考样本选择最短稳定建立时间，整帧超过 1.2 ms 或 ADC 超时即使运动锁
  保持闭合。全白、全黑和宽横线是合法光学图案，不是通信故障。
- BLS 不参与标定：短按松手请求起跑，长按约 1 s 只急停。当前固件将已归档的
  逐路白/黑记录编进程序，不再读取或写入末尾 Flash 标定区；普通 `.out` 下载无需
  再单独恢复标定。换灰度板、改变安装或 OUT 分压后，更新源码实测值并重新构建；
  每通道白黑跨度小于 ADC 满量程 10%、CRC 无效或横向坐标未登记时，禁止解锁 TB6612。
- 已归档标定（2026-07-30）：R0..R7 从行驶方向左到右，横向坐标依次
  `-35,-25,-15,-5,+5,+15,+25,+35 mm`；白/黑 ADC 依次为
  `174/4095,174/4095,172/4095,173/4095,171/4095,172/4095,171/4095,171/4095`，
  CRC=`0x74D9`。地址逻辑裕量、OUT 电压上限和 1000 帧建立时间仍须用仪器复核。
## 4. D36A 和球杆编码器接线

| 信号 | MSPM0 端 | D36A/编码器端 | 强制条件 |
|---|---|---|---|
| STEP | 当前未分配 | D36A ST1 | TB6612 占用 TIMA1，D36A 当前断开 |
| DIR | 当前未分配 | D36A DIR1 | TB6612 占用 PA13，需重新选脚 |
| EN | 当前未分配 | D36A EN1 | 与 STEP/DIR 一并重新分配后再验收 |
| Encoder A | PB18 GPIO | MS42CG A | C07A V1.1 引出；双边沿软件正交 |
| Encoder B | PB19 GPIO | MS42CG B | C07A V1.1 引出；与 A 成对 |
| Encoder PWM absolute | 当前未分配 | MS42CG PWM | TB6612 占用 PA14，MS42CG 当前断开 |
| Encoder Z | 暂不接 | MS42CG Z | 首版非必要 |
| Encoder VCC/GND | 3.3 V / GND | VCC/GND | **严禁 5 V** |
| 动力 | 12 V / GND | D36A VIN/GND | 动力粗线、星形回流 |

物理冲突限制：

- MPU6050 保留原接线：PA0=SDA、PA1=SCL、PA7=INT。
- 厂商 D36A 示例的 PA0/PA1 硬件 QEI 与 MPU6050 冲突，不能照搬；A/B 改到
  C07A V1.1 引出的 PB18/PB19，软件正交解码。
- PA13/PA14 已被当前 TB6612 左轮方向占用；D36A 的 DIR 与 MS42CG 的绝对 PWM
  在重新分配前不得接入。
- PA22 同时引到 CCD/J3-CN2；CCD 接口必须为空。
- 改 D36A 细分或限流拨码前必须断电，并同步更新固件每转脉冲数。

## 5. K230 与 MSPM0 串口接线

| CanMV K230 标准板 | C07A/MSPM0 | 说明 |
|---|---|---|
| UART2 TX / GPIO11 | PB7 / UART1_RX | 球状态帧，115200 8N1 |
| GND | GND | 必须共地 |
| UART2 RX / GPIO12 | 首版不接，PB6 保留 | 首版单向传输 |
| 3V3 | 不接 | 不并联两块板的电源 |

K230 背面 4P 座按板端丝印核对：
`GND / 3V3 / IO12-RX2 / IO11-TX2`。不得根据转接线颜色猜信号。

C07A 的 PB6/PB7 与板载蓝牙共用。CD4051 灰度换装后这对引脚不再由 Q2 使用，
但本次不自动接通 K230；K230 仍须在独立验收后才允许使用 PB7。蓝牙模块仍必须
物理拔下，只在软件里禁用不够。

首版 17 字节球状态帧：

```text
AA 55 | type=10 | seq | t_ms:u32 | x_mm:i16 | vx_mm_s:i16
      | confidence:u8 | flags:u8 | crc16:u16 | 0D
```

超过 120 ms 没有新鲜、CRC 正确且置信度合格的帧，MSPM0 必须让球杆目标角
缓慢回到 0°，车辆减速并最终停车。

## 6. K230 无线链路

```text
GC2093 / Sensor(id=2)
  ├─低分辨率 RGB/灰度通道 → 球检测 → UART2 → MSPM0
  └─1280×720 YUV420SP → 硬件 H.264 VENC → RTSP
       → K230 板载 2.4 GHz Wi-Fi STA
       → 2.4 GHz AP/热点（路由器、手机、电脑或 K230 AP）
       → 场外笔记本 VLC/OBS 显示、录像、回放
```

- 不使用赛场公共 Wi-Fi。外置路由器不是必购物料；有稳定 AP 即可。
- 使用外置 AP 时给 K230 做 DHCP 地址保留，接收地址固定为
  `rtsp://<K230_IP>:8554/test`。
- MSPM0 不等待 RTSP；断开图传不得影响 UART 帧龄和控制。
- SSID/密码只放在 SD 卡本地配置文件，不提交 Git。

## 7. 上电前检查

1. 整车通过 35 cm × 25 cm 检查框。
2. 不接电机动力，先用示波器检查 D36A `STEP/DIR/EN` 和失能安全态。
3. 确认编码器供电为 3.3 V，K230 UART 空闲高电平约 3.3 V。
4. 架空确认左右电机方向、编码器符号，再启用闭环。
5. 先跑静态球杆，再装车低速循迹。
6. 最终连续两小时运行球检测、UART、H.264/RTSP 和 OBS 录像。

更详细的架构、冲突说明和验收门见：
[`NUEDC2026_Car/docs/H2026_ARCHITECTURE.md`](NUEDC2026_Car/docs/H2026_ARCHITECTURE.md)。
