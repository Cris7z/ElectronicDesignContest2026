# K230 本地资料包审计与复用边界

> 审计日期：2026-07-31。资料包只保存在本机，不提交 Git；本文件是后续 Agent 的唯一可读索引。

## 1. 包级指纹与范围

| 项目 | 值 |
|---|---|
| 原始文件 | `C:\Users\Cris\Downloads\K230.zip` |
| ZIP 大小 | 329,744,499 B（314.469 MiB） |
| ZIP SHA-256 | `16f1a5fce04f77f251d8bb5191001e9ebe227417a6555d3d40d4c625661d3e3e` |
| 成员数 / 解压大小 | 561 / 681,776,344 B（650.193 MiB） |
| 完整性 | ZIP CRC 全部正常；未发现路径穿越、绝对路径或符号链接。 |

## 2. 已核对资产

| 资产 | 指纹 / 版本 | 当前结论 |
|---|---|---|
| 01Studio SD 镜像 | 650,117,120 B；SHA-256 `9be388ed57fec2d0ef642dff58e9d1ece5cf9fbf4b6ee03d998d4b485439b134`；文件名标注 CanMV K230 01Studio MicroPython v1.8 / nncase 2.11.0 | **直接采用**；先使用备用 SD 卡实机启动并记录版本。 |
| `gz (1)` 模型 | SHA-256 `4a67044838abe7e6e96ada50f823311552d4702b81d8440271f6e5987ed35f03` | **改造采用**；现有双点标定只作初值。 |
| `ball_all.py` 模型 | SHA-256 `7a879ad9a38902bf703f2f8de30e8b908d64ac52047a5c9e099eb1caea0fdb9a` | **候选比较**；上板加载后才可选用。 |
| 两份 `deploy_config.json` | 都声明 nncase 2.9.0 | 与 v1.8 / nncase 2.11.0 的兼容性必须实机验证。 |
| 497 张原图、36 张检测结果图 | 无 YOLO 标签、训练/验证划分、训练日志或 mAP 数据 | 只可作内部复现线索，**不能**作为可复算训练数据集。 |

## 3. 两套原型的复用判定

| 来源 | 可复用 | 禁止直接采用 |
|---|---|---|
| `gz (1)/main.py` | 1280×720 相机/ROI 思路、检测模型装载、像素到物理坐标的起点 | 两点钳位标定、明文网络配置、自定义 JPEG/TCP 推流、未使用的 8 位序号。 |
| `ball_all.py` | ROI、尺寸、置信度和时间连续性门控的思路 | F407/PD6、UART2 引脚、11 字节像素帧、8 位序号、旧固件初始化结论、旧 JPEG/TCP 接收器。 |
| 两套 `pc_receiver.py` | 仅作历史参考 | 不运行 `/deploy-main` 入口；不复用无认证的 HTTP 源码导出、浏览器内存录像或 UDP 探测死代码。 |
| PDF 教程 | 仅用于定位原始来源 | 不执行其中第三方 DLL 下载或写入 `System32` 的步骤。 |

## 4. 当前阶段的固定实现选择

1. 使用 v1.8 的官方 `PipeLine + Display.writeback + H.264 RTSP` 方式，不使用自定义 JPEG/TCP。
2. 从当前实机运行的脚本/模型导出 SHA-256 后作为视觉基线；压缩包模型只是候选资产。
3. 阶段 6 只输出内部 `VisionSampleV1` 和诊断 JSONL；不创建 STM32 UART 线序或帧定义。
4. 密码、模型、校准实参、录像、数据集和板端快照存放在 `D:\X\ElectronicDesignContest2026-Rebuild-local\k230\`，由 `.gitignore` 保护。

## 5. 上板必须记录的兼容性证据

- 板卡型号、GC2093 标签、实际 `Sensor(id=...)`、固件和 nncase 版本；
- 每个模型的加载结果、错误文本、检测 FPS、内存和温度；
- 相机安装、ROI、标定文件 SHA-256、模型 SHA-256；
- 无 RTSP 与 RTSP 同跑的性能差异。
