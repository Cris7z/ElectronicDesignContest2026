# 未决硬件问题与用户确认记录

> 状态：阶段 1 初版。  
> 规则：问题未关闭前，相关接线、IO、代码和“冻结文档”不得推进。

## Q-001：C07A3 代码基线

- 状态：**已关闭**。
- 用户确认：`c07a-line-tracker-preferred-20260731`。
- 固定提交：`0ef4fe0ac1f4fc11c8abcb3771d886cda78e2f65`。
- 影响：后续纯循迹迁移必须以该标签为源，不把原工作树的未提交改动混入基线。

## Q-002：TB6612 左右物理通道映射

- 状态：**已关闭**（2026-07-31，用户决定）。
- 说法 A（基线标签 E-01/E-02）：物理左轮为 TB6612 B 路（`PB3/TIMA1_CCP1 + PA16/PA17`），物理右轮为 A 路（`PB2/TIMA1_CCP0 + PA14/PA13`）。
- 说法 B（原工作树 2026-07-31 未提交接线表/AGENTS，E-03）：物理左轮为 A 路（`PB2/TIMA1_CCP0 + PA14/PA13`），物理右轮为 B 路（`PB3/TIMA1_CCP1 + PA16/PA17`）。
- 影响：电机方向、编码器符号、BSP、循迹算法输出、未来接线表和实车安全。
- 用户决定：以纯循迹标签为准，即物理左轮为 B 路、物理右轮为 A 路。
- 后续约束：迁移循迹代码、接线表和 BSP 均以该映射为准；任何新的实车矛盾必须重新打开本问题。

## Q-003：最终 K230 板卡型号

- 状态：**已关闭**（2026-07-31，用户决定）。
- 说法 A（基线文档 E-01/E-02）：01Studio CanMV K230 标准版，规划 1 GB，GC2093 70° 摄像头。
- 说法 B（订单截图 E-07，已人工核对）：正点原子 K230D BOX 标配 + GC2093 摄像头，订单状态“待发货”；本机另有对应 DNK230D 厂家资料包（E-06）。
- 影响：原理图、引脚、电源、UART、固件、摄像头、SDK、图传和后续所有 K230 代码。
- 用户决定：使用 01Studio CanMV K230。
- 后续约束：正点原子 K230D BOX 的截图和资料包只保留为历史采购线索；后续技术文档、原理图和代码不得混用其资料。

## Q-004：C07A 核心板准确版本

- 状态：**已关闭**（2026-07-31）。
- 用户确认：模块化底板为 **S27F**；核心板底部有两个按键。
- 厂家识别图结论：V1.1 只有 `RESET` 和 `BLS` 两个底部按键；V1.0 额外有 `USER` 键，共三个按键。
- 用户实物与 V1.1 相符，因此 C07A 核心板确定为 **V1.1**。
- 影响：后续核心板原理图、引脚引出与版本差异统一以 C07A V1.1 资料为准；实物电气异常仍可重新打开本问题。

## Q-005：D36A 实物版本和初始配置

- 状态：**部分关闭**。
- 用户确认：D36A 实物为 **V1.1**（2026-07-31，正反面照片）。
- 已知资料事实：V1.1 相比 V1.0 增加输入 ESD 与电源 TVS；`EN` 低电平休眠、高电平使能；`ST` 上升沿有效。
- 已补充：MS42CG 编码器资料已找到，默认输出 A/B、PWM、Z；其引脚不属于当前纯循迹 IO 方案。
- 仍待确认：当前细分拨码、当前限流拨码、配套步进电机额定电流、实物线束方向和接入前的失能安全态。
- 影响：D36A 技术文档、每转脉冲数、上电安全和未来 IO 分配。
- 在关闭前：D36A 保持断开，不写驱动、不分配正式 IO。

## Q-006：整车动力电源参数

- 状态：待后续测量。
- 需要确认：电池规格、保险值、线规、轮电机堵转峰值、步进锁止电流、K230 满载电流和 P03B 温升/纹波。
- 影响：供电树和最终整车安全；不阻塞当前纯循迹资料整理。

## Q-007：当前灰度循迹模块的赛题合规证据

- 状态：**已关闭**（2026-07-31，用户转述赛区确认）。
- 赛题原文要求“循迹模块只能使用红外光电模块，数量不限”。用户确认赛区已明确允许当前 CD4051 八路灰度循迹模块使用。
- 结论：当前模块可作为 H-R02 的允许循迹配置；不再因缺少商品型号、光学元件照片或候选网页详情而阻塞纯循迹迁移。
- 边界：仍须在官方尺寸场地完成一圈、停车、计时和不脱线实车验收，才能把 H-R02 标记为 `VERIFIED`。
