# RCT6 版本归档

本目录保存 2026-08-01 的实车调参历史。每个目录中的 `main_ball_beam_k230_control.c` 是当时源码快照，`ball_beam_rct6.bin` 是对应可烧录镜像。

## 正式版本

- `20260801_v16_q3_level_coast_hold/`
  - 正式活动源与该目录源码 SHA-256 完全一致。
  - BIN SHA-256：`7F728CCA6F625C273C94BD04D8F176491EB44D8C8F5E5C7123865F8FCD20BC93`。

## 历史版本

- V6：K3 RTSP 切换。
- V7：Q4 动态抗扰。
- V8：Q3 `-38 mm` 基线。
- V9–V15：预测制动、软制动、落点锁定与保持策略迭代。

这些版本用于回退和问题追溯，不是默认构建入口。`.elf` 和 `.map` 可以由工具链重新生成，不在版本归档中保存。
